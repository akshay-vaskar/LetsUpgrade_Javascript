// Question 2: Program to convert minutes to seconds

//Taking minutes from user
let minutes = prompt("Enter minutes to convert to seconds");

//Displaying the minutes converted to seconds
console.log("Seconds = "+ Math.floor(minutes*60))
